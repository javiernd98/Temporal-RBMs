This repo contains the base machinery for the implementation of kRBM (standard RBM plus the conditional sampling methodology) and the cRBM (conditional RBM). 
This base structure is further optimized and introduced into the broader repository of the group, inside the crbm fork
You can find this branch on: https://github.com/javiernd98/rbms/tree/crbms
